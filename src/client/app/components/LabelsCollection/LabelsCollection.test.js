
import React from 'react';
import { shallow } from 'enzyme';

import LabelsCollection from './LabelsCollection';

describe('LabelsCollection', () => {
    const wrapper = shallow(<LabelsCollection />);

    it('', () => {
        //
    });
});
